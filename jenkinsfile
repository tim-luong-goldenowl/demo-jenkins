pipeline {
    agent {
        docker {
            image 'node:lts-bullseye-slim'
            args '-u 0:0'
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'docker -v'
            }
        }
    }
}