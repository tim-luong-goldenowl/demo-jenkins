pipeline {
  agent {
    label 'linux-slave-01'
  }

  stages {
    stage('try check') {
      steps {
        echo('hahaae')
        publishChecks name: 'example'
        publishChecks name: 'example 2', detailsURL: "${env.RUN_DISPLAY_URL}"
      }
    }
  }
}