pipeline {
    agent {
        docker {
            image 'node:lts-bullseye-slim'
            args '-u 0:0 -v /tmp:/root/.cache'
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm test'
            }
        }
    }
}