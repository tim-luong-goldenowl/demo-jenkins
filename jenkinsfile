pipeline {
    agent any
    
    stages {
        stage('RANDOM LOGS') {
            steps {
                sh "echo THIS IS A LOG FROMMM ${GIT_BRANCH.tokenize('/').pop()}"
            }
        }
    }
}